# Ordria Web-App (Self-Order + Frontoffice + BackOffice)
# Dev: http://localhost:5173
#
# Base URL publik untuk QR meja self-order (dibuka HP pelanggan setelah scan).
# Contoh dev: kosongkan (= pakai window.location.origin, http://localhost:5173)
# Contoh prod: VITE_PUBLIC_BASE_URL=https://app.ordria.id
VITE_PUBLIC_BASE_URL=

# Base URL backend REST + Socket.io.
# Contoh dev: VITE_API_BASE_URL=http://localhost:4000
# Contoh prod: VITE_API_BASE_URL=https://api.ordria.id
VITE_API_BASE_URL=http://localhost:4000
